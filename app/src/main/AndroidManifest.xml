<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.xiaohu.devtest" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>
        <activity
            android:name=".MainActivitydragggg"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".HorizontalListViewDemo"
            android:label="@string/app_name" >

        </activity>
        <activity
            android:name=".Contextualactionbar"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".TestActivity"
            android:label="@string/app_name" >

        </activity>
        <activity
            android:name=".DrawerLayoutActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MoveAnimation"
            android:label="@string/app_name" >


        </activity>

        <activity android:name="com.facebook.FacebookActivity"
            android:configChanges=
                "keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:label="@string/app_name" />

    </application>

    <provider android:authorities="com.facebook.app.FacebookContentProvider1566118820308754"
        android:name="com.facebook.FacebookContentProvider"
        android:exported="true" />

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>
